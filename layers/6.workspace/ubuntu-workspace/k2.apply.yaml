k2:
  metadata:
    id: k.apps.anissa.layers.workspace.ubuntu-workspace
    kind: template-apply
  body:
    template:
      source: inventory
      params:
        id: k.anissa.recettes.docker-compose
    vars:
      name: kanissa-workspace
      title: Workspace
      description: Workspace conteneurisé , user HomeFolder
      username: tuxounet
      composeFile: |
        services:
          kanissa-workspace:
            build: 
              context: .
              args:
                USERNAME: tuxounet
            container_name: kanissa-workspace
            restart: unless-stopped
            environment:
              - TZ=UTC
              - USERNAME=tuxounet
            ports:
              - "${SSH_PORT:-2222}:2222"
            volumes:
              - ${KANISSA_DATA}/workspace:/workspace
              - ${KANISSA_DATA}/workspace-keypair:/etc/kanissa-ssh:ro
            networks:
              - anissa
        networks:
          anissa:
            external: true
